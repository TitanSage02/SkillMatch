services:
  - type: web
    name: rh-ai-agent-backend
    env: docker
    rootDir: backend
    envVars:
      - key: MISTRAL_API_KEY
        sync: false
      - key: ALLOWED_ORIGINS
        sync: false