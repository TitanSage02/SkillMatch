services:
  - type: web
    name: rh-ai-agent-backend
    env: docker
    dockerContext: ./backend
    dockerfilePath: Dockerfile
    envVars:
      - key: MISTRAL_API_KEY
        sync: false
      - key: ALLOWED_ORIGINS
        sync: false